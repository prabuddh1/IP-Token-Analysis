# Core
streamlit==1.37.1
altair==5.3.0

# Data stack
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
pyyaml==6.0.2

# DB / ORM
SQLAlchemy==2.0.32
psycopg[binary]==3.2.1    # v3 driver with manylinux wheels on Py 3.10–3.12
# (Intentionally NOT installing psycopg2-binary to avoid conflicts)

# Infra / utils
python-dotenv==1.0.1
requests==2.32.3
tenacity==8.5.0
web3==6.19.0

# Speedups when available
uvloop==0.19.0; sys_platform == 'linux'
